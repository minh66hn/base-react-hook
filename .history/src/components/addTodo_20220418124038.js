import React from "react";

const AddTodo = (props) => {
  return (
    <div>
        {
            handleClickbtn(event, todo){
                props.
            }
        }
    </div>
  );
};

export default AddTodo;
