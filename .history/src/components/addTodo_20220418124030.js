import React from "react";

const AddTodo = (props) => {
  return (
    <div>
        {
            handleClickbtn(event, todo){
                
            }
        }
    </div>
  );
};

export default AddTodo;
