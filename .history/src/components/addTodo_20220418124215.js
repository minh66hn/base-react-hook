import React from "react";

const AddTodo = (props) => {
  return (
    <div>
        {   
            const {hand}
            const handleClickbtn=(todo)=>{
                props.handleClickbtn( todo);
            }
        }
    </div>
  );
};

export default AddTodo;
