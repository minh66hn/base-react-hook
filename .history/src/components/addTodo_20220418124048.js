import React from "react";

const AddTodo = (props) => {
  return (
    <div>
        {
            handleClickbtn( todo){
                props.handleClickbtn( todo);
            }
        }
    </div>
  );
};

export default AddTodo;
